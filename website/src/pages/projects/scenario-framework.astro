---
import MainLayout from '../../layouts/MainLayout.astro';
---

<MainLayout title="仿真场景泛化 | 王江涛">
  <main class="pt-14 min-h-screen">
    <div class="max-w-4xl mx-auto px-6 py-16">
      <a href="/#projects" class="text-sm font-mono text-muted hover:text-black transition-colors">
        ← 返回项目列表
      </a>

      <h1 class="mt-8 text-3xl font-sans font-bold">自动驾驶仿真场景泛化与标准化生成</h1>
      <p class="mt-2 text-xs font-mono text-muted">核心技术：Python、esmini、OpenSCENARIO、JSON Schema、Scenario Generation、Simulation Replay、Rule-based Parameterization</p>

      <div class="mt-8 space-y-6">
        <section>
          <h2 class="text-lg font-sans font-semibold">项目定位</h2>
          <p class="mt-2 text-muted leading-relaxed">
            设计面向自动驾驶测试场景的统一 JSON Schema，构建 JSON → OpenSCENARIO 转换链路，支持场景参数泛化与回归评测，实现真实 Bad Case 的结构化表达与场景资产化。
          </p>
        </section>

        <section>
          <h2 class="text-lg font-sans font-semibold">核心功能</h2>
          <ul class="mt-3 space-y-3 text-muted leading-relaxed">
            <li>
              <strong>场景中间层设计：</strong>设计面向自动驾驶测试场景的统一 JSON Schema，抽象 ego、目标车、道路拓扑、行为序列、触发条件、时序事件与评测指标等核心字段，实现真实 Bad Case 的结构化表达与场景资产化
            </li>
            <li>
              <strong>Scenario 生成与仿真执行：</strong>构建 JSON → OpenSCENARIO 转换链路，支持生成 esmini 可执行场景，完成 Cut-in、低 TTC 跟驰、异常减速、路口交互等典型风险场景的自动化 Replay
            </li>
            <li>
              <strong>场景参数泛化：</strong>支持对速度、相对距离、TTC、切入角度、起始位置与目标行为等关键参数进行规则化采样与变异，生成同一 Bad Case 的多版本 Scenario Family，用于鲁棒性评测与风险边界探索
            </li>
            <li>
              <strong>回归评测与 KPI 分析：</strong>基于 esmini headless 模式批量执行仿真场景，自动统计 collision、min TTC、距离变化等关键 KPI，支持 Regression Replay 与失败场景回归分析
            </li>
            <li>
              <strong>数据闭环联动：</strong>支持接收 Trigger Engine 与感知评测工具输出的高价值 Bad Case，将真实失败场景转化为可复现、可泛化的 Simulation Scenario Asset，用于后续评测集扩充与回归测试
            </li>
          </ul>
        </section>

        <section>
          <h2 class="text-lg font-sans font-semibold">技术栈</h2>
          <div class="mt-2 flex flex-wrap gap-2">
            {['Python', 'esmini', 'OpenSCENARIO', 'JSON Schema', 'Scenario Generation', 'Simulation Replay', 'Rule-based Parameterization'].map((tag) => (
              <span class="text-xs font-mono px-2 py-1 bg-surface border border-border rounded text-muted">
                {tag}
              </span>
            ))}
          </div>
        </section>
      </div>
    </div>
  </main>
</MainLayout>
